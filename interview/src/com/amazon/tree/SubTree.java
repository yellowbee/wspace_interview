package com.amazon.tree;

/*
 * Given two very large binary trees T1, with millions of nodes,
 * and T2, with hun- dreds of nodes, create an algorithm to
 * decide if T2 is a subtree of T1.
 * 
 * Solution: Get inorder and preorder of both trees, if both in and pre
 * of one tree are substrings of in and pre, respectively, of another tree,
 * then one tree is the subtree of another tree.
 */

public class SubTree {

}
