package com.amazon.llist;

public class LinkedListUtilities {

	/*
	 * Write an algorithm to determine if 2 linked lists intersect.
	 * 
	 * Solution: Put references of all the elements from one list
	 * in a hash, then go through the other list.
	 */
}
